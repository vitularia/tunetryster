<%= form_for(@tune) do |f| %>
  <% if @tune.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tune.errors.count, "error") %> prohibited this tune from being saved:</h2>

      <ul>
      <% @tune.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Tune Name</strong><br>
    <%= f.text_area :tunename %>
  </div>
  <div class="field">
    <strong>Key</strong><br>
    <%= f.text_field :tunekey %>
  </div>

  <div class="field">
    <strong>Source</strong><br>
    <%= f.text.field :tunesource %>
  </div>

  <div class="field">
    <strong>Source Recording</strong><br>
    <%= f.text_area :sourcerecording %>
  </div>

  <div class="field">
    <strong>Notes</strong><br>
    <%= f.text_area :tunenotes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
